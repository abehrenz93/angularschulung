  testing

#  Tag 2 Notzien
- Notizen : Day 2
    Karma, Jasmine, jest
    describe, it, expect(Assertion)

  Regel für Tests, kein und , oder im should verwenden somit könnte man
  verhindern dass man mehrere Testfälle in einem test testet


  - Immutability:
  Für Object und Arrays gilt: IMMER kopien erzeugen uind Neemals werte direkt ändern
    --> um keine Seiteneffekte zu erzeugen
    --> bessere Performance, ChangeDetection muss nicht alles nochmal prüfen
    --> Grundvoraussetzung für Redux
    --> spreadOperator zum kopieren , deepCopy mit structuredClonse(OBJ) // nur ganz selten
        * Shadow Copy, Shallow Copy

        * Inject statt new Service .... dann haben wir nur eine Instance
        und initialisieren nicht viele Instancen durch das nutzen in mehreren Komponenten

  Pipes müssen importiert werden, verändern die EIngabe, können verkettet werden | Uppercase, lowercase, number, number:'1.2-2' etc.
    CurrencyPipe , DatePipe , JsonPipe, SlicePipe, KeyValuePipe, AsyncPipe

#  Tag 3 Notizen
- Notizen : Day 3
    Tree-Shakable Providers: providedIn: root ... statt im app.config.ts / NgModule ein Import zu machen
    SpyOn im Test , return undefinded kann man mit CallTrough() beheben

     Change Detection: Wann ändern sich Daten ?
      - Event Binding / HTTP / WEBSOCKET / Timer/ Intervall [@Input(), AsyncPipe, manuell, Promise, async await , Signal ]
        Zone.js, prüft das gesamte Dokument auf veränderungen

        changeDetection wird nur in Komponenten ausgelöst die auch mit der Strategie OnPush

        Signals
        Observables, subscribe


#  Tag 4 Notizen
- Notizen : Day 4
    Race Condition --> Bei 2 Requests, der erste länger benötigt den zweiten nach dem ersten anzeigt.
     --> dafür gibt es 4 RxJs Operatoren um das Problem zu lösen

    Formulare: Austausch , Reaktion , Validierung , Feedback
    FormGroup, FormArray, FormControl, validators
    -- Validierungsfehler abrufen mit zb. control.hasError(errorCode), control.getError(errorCode), control.errors

  #  Tag 5 Notizen
- Notizen : Day 5

  #  Tag 6 Notizen
- Notizen : Day 6










